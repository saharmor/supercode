# SuperCode Configuration File
# This is an example configuration - copy to .env and update with your own values

# Speech Recognition Service Configuration
# Set to 'true' to use OpenAI's Whisper API (more accurate but requires API key)
# Set to 'false' to use Google's speech recognition (less accurate but free)
USE_OPENAI_API=false

# Your OpenAI API key (required if USE_OPENAI_API=true)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here # used for computer use, specifically capture coordinates of UI elements

# OpenAI Whisper model to use (whisper-1 is the default)
OPENAI_TRANSCRIPTION_MODEL=whisper-1

# Audio configuration
AUDIO_DEVICE_INDEX=2
MAX_RECORDING_TIME_SEC=30

# Google Gemini API key (for screenshots and image analysis)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Default IDE to use when SuperCode starts
# Options: windsurf, cursor, lovable
DEFAULT_IDE=windsurf

# Flag to enable prompt enhancement (improves AI responses for coding)
# Set to "true" to enable or "false" to disable
ENHANCE_PROMPT=true
